{
  "name": "Claude Skills Executor",
  "description": "Provides MCP tools for repository automation commands (validate-skills, sample artifact replay).",
  "command": [
    "uvx",
    "--from",
    "path",
    "./mcp/servers/skill_executor/server.py"
  ],
  "env": {
    "AISKILLIN_REPO_ROOT": "${WORKSPACE_ROOT}"
  }
}
